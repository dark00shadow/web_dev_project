/* register.js - Specific logic for register page */
window.onload = function() {
    // If opened as a popup, smoothly resize to the appropriate dimensions
    if (window.name === 'popup') {
        smoothResizeTo(520, 800);
    }
};

